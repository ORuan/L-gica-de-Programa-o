#include<stdio.h>
#include<stdlib.h>

float somaF(float soma[3]);
    int i=0;
    float resultado=0;
    while (i<3){
        scanf("%f", &soma[i]);
        media += soma[i];
        i++;
    }
    return (media/i);
}
main(){
    float soma[3],resultado;
    int i;
    resultado = somaF(soma[3,resultado]);
    printf("%f", resultado));
}
